# ZindeAI_app (Flutter)

MVP shell:
- Login (local config)
- Generate Plan (POST)
- Timeline (render response)

## Local
1) Copy `.env.example` -> `.env` and set values
2) `flutter pub get`
3) `flutter run`
